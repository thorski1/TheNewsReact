import { makeStyles } from "@material-ui/core/styles";

export const useCollectionGridStyles = makeStyles(
	theme => ({
		root: {
			flexGrow: 1,
			marginTop: 40,
			justifyContent: "center"
		}
	})
);
